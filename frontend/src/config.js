// Site-wide configuration values. Edit and rebuild to update.
// Dates are ISO YYYY-MM-DD; rendered with Intl in the UI.

export const config = {
  // Date the score CSVs in public/data were last refreshed.
  leaderboardLastUpdated: '2026-05-29',
}
